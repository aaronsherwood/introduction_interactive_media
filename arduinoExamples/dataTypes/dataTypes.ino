//a number with no decimal point (16 bits in size)
int myNumber = 200;

//integer that is constant
const int ledPin = 2;

//decimal point (32 bits large)
float myFloatingPointValue = 1.25;

//boolean
bool yesNo = false;

//char is one character (it's also the size of 1 byte, or 8 bits)
char myChar = 'a';

//string= bunch of chars
String myString = "Welcome to Intro to IM";

//can hold anything with 256 values (8 bits)
byte myByte = 255; // or:
byte a = 'a'; //(remember a char is the size of one byte, so can be stored in a byte)

//a long is a big int (32 bits)
long bigNumber = 1000; 

//unsigned longer can go to a bigger positive number because there is no negative sign stored
unsigned long evenBiggerNumber;

//even BIGGER - 64 bits
long long reallyBIGnumber;

//a bigger float (64 bits)
double bigFloat = 100000.001;


void setup() {
  // put your setup code here, to run once:
 
}

void loop() {
  // put your main code here, to run repeatedly:

}
